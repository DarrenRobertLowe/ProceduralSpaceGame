///getGridPosition();
/* Gets the column and row from the current x and y and
 * sets those fields, so this method should only be
 * called by GRID_OBJECTs.
 */

column = getColumnFromX(x);
row = getRowFromY(y);
